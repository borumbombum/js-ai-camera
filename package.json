{
  "name": "js-ai-camera",
  "version": "1.0.4",
  "description": "AI-powered camera object detection",
  "scripts": {
    "server": "cd server && ../.venv/bin/python stream_server.py",
    "stop": "lsof -ti :4422 | xargs kill 2>/dev/null || true",
    "restart": "npm run stop && sleep 1 && npm run server &"
  }
}
